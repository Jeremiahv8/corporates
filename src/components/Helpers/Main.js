import { connect } from "react-redux";
import { Container, Row, Col } from "react-bootstrap";


const toCurrency = ({ value }) => {
    let Currency = "XXX"+ value;
    console.log(Currency);
    return <>a</>;
};



export default toCurrency;
